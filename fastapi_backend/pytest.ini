[pytest]
asyncio_mode = auto

# Coverage settings
[coverage:run]
source = app
omit =
    */tests/*
    */__pycache__/*
    */venv/*
    */.venv/*

[coverage:report]
precision = 2
fail_under = 80
show_missing = True
skip_covered = False

[coverage:html]
directory = htmlcov